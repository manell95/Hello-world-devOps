📊 Vue d'Ensemble de l'Architecture
┌─────────────────────────────────────────────────────────────────┐
│                        DÉVELOPPEUR                               │
│                                                                  │
│  ┌────────────┐        ┌────────────┐        ┌────────────┐   │
│  │   Coding   │───────▶│  Git Commit│───────▶│  Git Push  │   │
│  └────────────┘        └────────────┘        └─────┬──────┘   │
└────────────────────────────────────────────────────┼───────────┘
                                                      │
                                                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                      GITHUB REPOSITORY                           │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │              GitHub Actions Workflow                  │      │
│  │                                                       │      │
│  │  Step 1: Checkout Code                              │      │
│  │  Step 2: Validate Syntax                            │      │
│  │  Step 3: Run Tests (future)                         │      │
│  │  Step 4: Connect to EC2 via SSH                     │      │
│  │  Step 5: Pull Latest Code                           │      │
│  │  Step 6: Install Dependencies                       │      │
│  │  Step 7: Restart Application                        │      │
│  └───────────────────────┬──────────────────────────────┘      │
└──────────────────────────┼─────────────────────────────────────┘
                           │ SSH Connection
                           │ (Port 22)
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                     AWS CLOUD (eu-west-3)                        │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                    VPC (Default)                        │    │
│  │                                                         │    │
│  │  ┌───────────────────────────────────────────────┐    │    │
│  │  │            Security Group                      │    │    │
│  │  │  • Inbound: Port 22 (SSH)                     │    │    │
│  │  │  • Inbound: Port 80 (HTTP)                    │    │    │
│  │  │  • Inbound: Port 3000 (Node.js)               │    │    │
│  │  │  • Outbound: All traffic                      │    │    │
│  │  └───────────────────┬───────────────────────────┘    │    │
│  │                      │                                  │    │
│  │  ┌───────────────────▼───────────────────────────┐    │    │
│  │  │        EC2 Instance (t2.micro)                 │    │    │
│  │  │        Ubuntu 20.04 LTS                        │    │    │
│  │  │                                                │    │    │
│  │  │  ┌──────────────────────────────────────┐    │    │    │
│  │  │  │   User Data Script                    │    │    │    │
│  │  │  │   (Executed at boot)                  │    │    │    │
│  │  │  │                                       │    │    │    │
│  │  │  │   1. Update system packages           │    │    │    │
│  │  │  │   2. Install Node.js v18              │    │    │    │
│  │  │  │   3. Install Git                      │    │    │    │
│  │  │  │   4. Clone repository                 │    │    │    │
│  │  │  │   5. Install npm dependencies         │    │    │    │
│  │  │  │   6. Start Node.js server             │    │    │    │
│  │  │  └──────────────────────────────────────┘    │    │    │
│  │  │                                                │    │    │
│  │  │  ┌──────────────────────────────────────┐    │    │    │
│  │  │  │   Node.js Application                 │    │    │    │
│  │  │  │                                       │    │    │    │
│  │  │  │   • Express Server                    │    │    │    │
│  │  │  │   • Port 3000                         │    │    │    │
│  │  │  │   • Serves index.html                 │    │    │    │
│  │  │  │   • API Endpoints                     │    │    │    │
│  │  │  └──────────────────────────────────────┘    │    │    │
│  │  └────────────────────────────────────────────────┘    │    │
│  └──────────────────────────────────────────────────────────────┘
└──────────────────────────┬─────────────────────────────────────┘
                           │ HTTP Response
                           │ (Port 3000)
                           ▼
                    ┌─────────────┐
                    │  END USERS  │
                    └─────────────┘

🔄 Flux de Déploiement Détaillé
Séquence de Déploiement Automatique
mermaidsequenceDiagram
    participant Dev as Développeur
    participant Git as GitHub
    participant GHA as GitHub Actions
    participant EC2 as Instance EC2
    participant App as Node.js App

    Dev->>Git: git push origin master
    Git->>GHA: Trigger workflow (on push)
    
    Note over GHA: Étape 1: Checkout
    GHA->>Git: Clone repository
    
    Note over GHA: Étape 2: Validation
    GHA->>GHA: Validate syntax
    
    Note over GHA: Étape 3: SSH Connection
    GHA->>EC2: Connect via SSH (port 22)
    EC2-->>GHA: Connection established
    
    Note over GHA,EC2: Étape 4: Déploiement
    GHA->>EC2: cd ~/Hello-world-devOps
    GHA->>EC2: git pull origin master
    EC2->>Git: Fetch latest code
    Git-->>EC2: Code downloaded
    
    GHA->>EC2: npm install
    EC2->>EC2: Install dependencies
    
    GHA->>EC2: pm2 restart all || node server.js
    EC2->>App: Restart application
    
    App-->>EC2: Application running
    EC2-->>GHA: Deployment successful
    GHA-->>Git: Update workflow status ✅
    Git-->>Dev: Notification (success)

🧩 Composants Détaillés
1. GitHub Repository
Rôle : Héberger le code source et déclencher les workflows
Composants clés :

.github/workflows/deploy.yml : Définition du pipeline CI/CD
server.js : Code de l'application
user-data.sh : Script d'initialisation EC2
package.json : Dépendances Node.js

Triggers :
yamlon:
  push:
    branches: [ master ]
  workflow_dispatch:  # Déclenchement manuel

2. GitHub Actions
Rôle : Orchestrer le processus de déploiement
Étapes du workflow :
1. Checkout Code
   └─▶ actions/checkout@v3
   
2. Setup SSH
   └─▶ Configure private key
   └─▶ Add EC2 to known_hosts
   
3. Deploy to EC2
   └─▶ SSH Connection
   └─▶ Pull code
   └─▶ Install dependencies
   └─▶ Restart server
   
4. Health Check
   └─▶ Verify application is running
   └─▶ Test HTTP endpoint
Secrets utilisés :

EC2_PRIVATE_KEY : Clé SSH
EC2_HOST : IP de l'instance
EC2_USER : Nom d'utilisateur SSH
AWS_ACCESS_KEY_ID : Credentials AWS
AWS_SECRET_ACCESS_KEY : Credentials AWS


3. AWS EC2 Instance
Spécifications :

Type : t2.micro (1 vCPU, 1 GB RAM)
OS : Ubuntu 20.04 LTS
Storage : 8 GB EBS (General Purpose SSD)
Région : eu-west-3 (Paris)

Software installé :
bash- Node.js v18.x
- npm v9.x
- Git v2.x
- PM2 (process manager, optionnel)
Ports ouverts :
PortProtocolServiceSource22TCPSSHVotre IP80TCPHTTP0.0.0.0/03000TCPNode.js0.0.0.0/0

4. Application Node.js
Stack technique :
┌─────────────────┐
│  HTTP Requests  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Express.js     │  ← Framework web
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Routes         │  ← /,  /health, /api/*
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Controllers    │  ← Logique métier
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Response       │  ← JSON ou HTML
└─────────────────┘
Structure des endpoints :
javascriptGET  /           → index.html (page d'accueil)
GET  /health     → { status: "ok", uptime: 123 }
GET  /api/status → { version: "1.0", env: "production" }
POST /api/data   → (future endpoint)

🔐 Sécurité de l'Architecture
Couches de Sécurité
┌─────────────────────────────────────┐
│  Layer 1: GitHub Secrets            │  ← Credentials chiffrés
├─────────────────────────────────────┤
│  Layer 2: AWS IAM                   │  ← Permissions minimales
├─────────────────────────────────────┤
│  Layer 3: Security Groups           │  ← Firewall réseau
├─────────────────────────────────────┤
│  Layer 4: SSH Keys                  │  ← Authentification forte
├─────────────────────────────────────┤
│  Layer 5: Application (Node.js)     │  ← Validation des entrées
└─────────────────────────────────────┘
Flux d'Authentification
Developer Push
      │
      ▼
┌──────────────────┐
│ GitHub verifies  │
│ user identity    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ GitHub Actions   │
│ uses secrets     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ SSH to EC2 with  │
│ private key      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ EC2 validates    │
│ SSH key          │
└────────┬─────────┘
         │
         ▼
    Deployment ✅

📈 Scalabilité Future
Architecture Actuelle vs Future
┌────────────────────────────────────────────────────────────┐
│                    CURRENT (v1.0)                           │
├────────────────────────────────────────────────────────────┤
│  Single EC2 Instance                                        │
│  Manual scaling                                             │
│  No load balancing                                          │
│  Single region                                              │
└────────────────────────────────────────────────────────────┘
                            │
                            │ Evolution
                            ▼
┌────────────────────────────────────────────────────────────┐
│                    FUTURE (v3.0)                            │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────┐     │
│  │  Route 53 (DNS)                                   │     │
│  └─────────────────────┬────────────────────────────┘     │
│                        │                                    │
│  ┌─────────────────────▼────────────────────────────┐     │
│  │  CloudFront (CDN)                                 │     │
│  └─────────────────────┬────────────────────────────┘     │
│                        │                                    │
│  ┌─────────────────────▼────────────────────────────┐     │
│  │  Application Load Balancer                        │     │
│  └──┬──────────────┬──────────────┬─────────────────┘     │
│     │              │              │                         │
│  ┌──▼───┐      ┌──▼───┐      ┌──▼───┐                    │
│  │ ECS  │      │ ECS  │      │ ECS  │  (Containers)       │
│  │Task 1│      │Task 2│      │Task 3│                     │
│  └──────┘      └──────┘      └──────┘                     │
│     │              │              │                         │
│  ┌──▼──────────────▼──────────────▼───┐                   │
│  │  RDS (PostgreSQL) Multi-AZ          │                   │
│  └─────────────────────────────────────┘                   │
│                                                             │
└────────────────────────────────────────────────────────────┘

🔄 Workflow d'Initialisation (User Data)
EC2 Boot
   │
   ▼
┌────────────────────────┐
│ Execute user-data.sh   │
└──────────┬─────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Step 1: System Update                   │
│   apt-get update && apt-get upgrade     │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Step 2: Install Node.js                 │
│   curl -fsSL nodesource | bash          │
│   apt-get install -y nodejs             │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Step 3: Install Git                     │
│   apt-get install -y git                │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Step 4: Clone Repository                │
│   git clone repo-url                    │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Step 5: Install Dependencies            │
│   cd project && npm install             │
└──────────┬──────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Step 6: Start Application               │
│   node server.js &                      │
└──────────┬──────────────────────────────┘
           │
           ▼
     Application Running ✅

💾 Stockage et Données
Structure des Données (Actuelle)
EC2 Instance
│
├── /home/ubuntu/
│   └── Hello-world-devOps/
│       ├── node_modules/        (dependencies)
│       ├── server.js            (application)
│       ├── index.html           (static files)
│       ├── package.json         (config)
│       └── .git/                (version control)
│
└── /var/log/
    └── cloud-init-output.log    (boot logs)
Future: Séparation des Données
┌─────────────────────┐
│  ECS Tasks (App)    │  ← Stateless
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  RDS (Database)     │  ← Persistent data
└─────────────────────┘

┌─────────────────────┐
│  S3 (Static Files)  │  ← Media & assets
└─────────────────────┘

┌─────────────────────┐
│  ElastiCache Redis  │  ← Session & cache
└─────────────────────┘

📊 Monitoring et Observabilité
Monitoring Stack (Future)
┌─────────────────────────────────────────┐
│           CloudWatch                     │
│                                          │
│  ┌────────────────────────────────┐    │
│  │  Metrics                        │    │
│  │  • CPU Utilization              │    │
│  │  • Memory Usage                 │    │
│  │  • Network Traffic              │    │
│  │  • Disk I/O                     │    │
│  └────────────────────────────────┘    │
│                                          │
│  ┌────────────────────────────────┐    │
│  │  Logs                           │    │
│  │  • Application logs             │    │
│  │  • Error logs                   │    │
│  │  • Access logs                  │    │
│  └────────────────────────────────┘    │
│                                          │
│  ┌────────────────────────────────┐    │
│  │  Alarms                         │    │
│  │  • High CPU → SNS → Email       │    │
│  │  • 5xx Errors → Slack           │    │
│  │  • Disk Full → PagerDuty        │    │
│  └────────────────────────────────┘    │
└─────────────────────────────────────────┘

🔀 Décisions d'Architecture
Pourquoi ces choix ?
DécisionJustificationNode.jsLéger, rapide, adapté aux APIs RESTEC2 vs LambdaContrôle total, coût prévisible pour démoGitHub Actions vs JenkinsGratuit, intégré, simple à configurerShell scripts vs AnsibleSimplicité, pas de dépendancesUbuntu 20.04LTS, bien documenté, compatible

🎯 Roadmap Architecture
Phase 1 (Actuel) ✅

Single EC2 instance
GitHub Actions CI/CD
Manual scaling

Phase 2 (3-6 mois)

Docker containers
ECS deployment
RDS database
CloudWatch monitoring

Phase 3 (6-12 mois)

Multi-region
Auto-scaling
Load balancer
CDN (CloudFront)


Ce document est vivant et sera mis à jour au fur et à mesure de l'évolution du projet.
